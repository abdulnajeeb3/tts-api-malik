# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
websockets==13.1
python-multipart==0.0.12
pydantic==2.9.2
pydantic-settings==2.6.0

# --- ML runtime ---
# Torch is installed from the CUDA index in the Dockerfile (not here), so
# the Docker build picks the CUDA build and local pip installs the CPU wheel.
torch==2.4.1
transformers==4.46.0
accelerate==1.0.1
safetensors==0.4.5
huggingface_hub==0.26.0
sentencepiece==0.2.0

# --- Audio ---
numpy==1.26.4
soundfile==0.12.1
librosa==0.10.2.post1
pydub==0.25.1
# Opus encoding (requires libopus system package)
opuslib==3.0.1

# --- Observability ---
structlog==24.4.0
python-json-logger==2.0.7

# --- Dev / benchmark ---
httpx==0.27.2
rich==13.9.2
